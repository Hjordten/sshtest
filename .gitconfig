[user]
	name = Hjordten
	email = andr314e@stud.kea.dk
[alias]
	co = checkout
	root = rev-parse --show-toplevel
	undo = reset --soft HEAD^
	tree = log --oneline --all --graph
	mergetest = "!f(){ git merge --no-commit --no-ff \"$1\"; git merge --abort; echo \"Merge aborted\"; };f "
